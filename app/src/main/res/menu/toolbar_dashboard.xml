<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:ucoin="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity"
    >


    <item android:id="@+id/action_search"
        android:title="@string/search"
        android:icon="@drawable/ic_search_white_24dp"
        ucoin:iconifiedByDefault="false"
        ucoin:showAsAction="always"
        ucoin:actionViewClass="android.support.v7.widget.SearchView" />

    <item android:id="@+id/action_add_wallet"
          android:title="@string/favorites"
          android:icon="@drawable/ic_menu_add_wallet"
          ucoin:iconifiedByDefault="true"
          ucoin:showAsAction="always"/>

    <item android:id="@+id/action_contact_list"
        android:title="@string/favorites"
        android:icon="@drawable/ic_menu_friendslist"
        ucoin:iconifiedByDefault="true"
        ucoin:showAsAction="always"/>

</menu>


